import { StaffFilterPipe } from './staff-filter.pipe';

describe('StaffFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StaffFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
